# project-youtube-insight

## 프로젝트 정보

- **Discord 스레드**: https://discord.com/channels/1488217973893365902/1541026328646647899
- **스레드 ID**: 1541026328646647899
- **생성일**: 2026-08-23
- **작업 디렉토리**: /Users/mh97888/business/projects/project-youtube-insight
- **Notion 레지스트리 행**: 

## 한 줄 목적



<!-- 비어 있으면 첫 작업에서 사용자에게 물어 채운다. 목적을 못 쓰는 요청은 프로젝트가 아니라 단발 질문이다. -->

---

## 작업 시작 전 반드시

1. `.agent-log.md`를 읽어 이전 진행 상황을 파악한다.
2. Notion 레지스트리 행의 `다음 행동`과 `지금 막힌 질문`을 확인한다.
3. 새 요청이 이전 작업의 연속이면 맥락을 유지해 이어서 진행한다.

---

## 응답 규약 — 이 프로젝트는 모바일 Discord에서 읽힌다

읽는 사람은 대부분 폰을 보고 있다. 긴 응답은 읽히지 않고, 읽히지 않은 응답은 없는 것과 같다.

- **Discord 응답은 15줄 이내.** 표는 3열까지. 코드블록은 20줄까지.
- **긴 산출물은 Discord에 쓰지 않는다.** 파일로 쓰거나 Notion 페이지로 만들고, 응답에는 링크와 3줄 요약만 남긴다.
- 진행 과정 설명 금지. 무엇이 달라졌는지만 쓴다.
- 끝에 항상 다음 한 줄: `다음: <이어서 할 일 한 문장>`

### 나쁜 예

> 먼저 파일을 읽고 구조를 파악한 뒤 A를 수정했고 그 다음 B도 함께 손봐야 해서... (40줄)

### 좋은 예

> ✅ 로그인 만료 처리 추가 (`auth.go`, 테스트 3개 통과)
> ⚠️ 리프레시 토큰 저장소가 아직 인메모리 — 재시작하면 로그아웃됨
> 다음: 토큰 저장소를 SQLite로 옮길지 결정 필요

---

## 질문 프로토콜 — 막히면 멈추고 되묻는다

추측해서 진행하지 않는다. 되돌리는 비용이 묻는 비용보다 크다.

**단, 묻기 전에 지켜야 할 것:**

- 스스로 알아낼 수 있는 것은 묻지 않는다. 파일을 읽어보면 아는 것, 검색하면 나오는 것은 직접 확인한다.
- 답변에 의존하지 **않는** 작업은 먼저 다 끝내고 묻는다. 답변을 기다리는 동안 아무것도 안 되어 있으면 안 된다.
- 한 응답에 질문 블록은 **하나만**. 선택지는 최대 3개.
- 모든 선택지에 **결과**를 붙인다. 선택지만 나열하면 답할 수 없다.
- **기본값을 반드시 선언한다.** 사용자가 `알아서`라고 답하면 기본값으로 진행한다.

### 형식 — 이 형식을 정확히 지킨다

```
❓ 결정 필요
<질문 한 문장>

1) <선택지> — <이걸 고르면 어떻게 되는지>
2) <선택지> — <이걸 고르면 어떻게 되는지>
3) <선택지> — <이걸 고르면 어떻게 되는지>

기본값: 2 (숫자만 보내거나 `알아서`라고 답하면 됩니다)
```

### 이미 해둔 것을 먼저 쓴다

```
✅ 스키마 마이그레이션 작성 완료, 테스트 통과
⏸ 아래 결정 후 배포 스크립트 진행

❓ 결정 필요
기존 데이터를 어떻게 처리할까요?

1) 전체 백필 — 안전하지만 배포 중 5분 정지
2) 지연 마이그레이션 — 무중단이지만 코드가 두 형식을 6개월 지원
3) 폐기 후 재생성 — 가장 빠르지만 2월 이전 기록 손실

기본값: 2 (숫자만 보내거나 `알아서`라고 답하면 됩니다)
```

---

## Notion 연동 규약

`.mcp.json`으로 Notion MCP가 연결되어 있다.

- 작업이 끝나면 **프로젝트 레지스트리** 행의 `상태`, `마지막 활동`, `다음 행동`을 갱신한다.
- 질문을 남기고 멈췄으면 `상태`를 `대기 중 — 내 답변 필요`로 바꾸고 `지금 막힌 질문`에 질문 한 문장을 쓴다.
- 답변을 받아 진행을 재개하면 `지금 막힌 질문`을 비우고 `상태`를 `활성`으로 되돌린다.
- 긴 설계 문서·조사 결과는 레지스트리 행의 하위 페이지로 만든다. **코드 설명을 Notion에 복사하지 않는다** — 곧 git과 어긋난다. 포인터만 둔다.

### 레지스트리 위치

- 데이터소스: `collection://0fb0c45a-bbb6-4ad5-98bf-ccd1887fcdce`
- 페이지: <https://app.notion.com/p/f7a8906bb5784d35a59e86c4534e1a87>

---

## 일정 규약 — 기한은 캘린더에만 있다

Notion `기한` 칸과 캘린더 이벤트가 어긋나면 캘린더가 맞다. 알림이 오는 쪽이 진실이다.

기한이 생기면 `gcalcli`로 이벤트를 만들고 레지스트리의 `캘린더 등록`을 체크한다.

```bash
gcalcli --nocache add \
  --title "[project-youtube-insight] ⚠️ 마감 — 청첩장 최종본 전달" \
  --when "2026-08-20 21:00" --duration 60 \
  --color tomato \
  --description "스레드: https://discord.com/channels/1488217973893365902/1541026328646647899" \
  --noprompt
```

**`quick`이 아니라 `add`를 쓴다.** `quick`은 `--color`를 받지 않아 색 규약을 지킬 수 없다.

### 색 규약 — 폰 캘린더에서 한눈에 구분하기 위한 것

| 언제 | 색 | 왜 |
|---|---|---|
| **마감·데드라인** (분류 무관) | `tomato` (빨강) | 놓치면 안 되는 것. 빨강은 이 용도로만 쓴다 |
| 사업 | `peacock` (청록) | |
| 투자·자본 | `basil` (진초록) | 돈 |
| 게임 | `grape` (보라) | |
| 개인·생활 | `flamingo` (분홍) | |
| 학습 | `banana` (노랑) | |
| 실험 | `tangerine` (주황) | |

분류는 Notion 레지스트리의 `분류` 칸을 따른다. 모르면 사용자에게 묻지 말고 `graphite`(회색)로 만들고, 나중에 분류가 정해지면 고친다.

`lavender`·`sage`·`blueberry`는 비워 둔다 — 나중에 규약이 늘어날 자리다.

- 이벤트 제목은 `[프로젝트명]`으로 시작한다. 캘린더에서 프로젝트 단위로 걸러 보기 위한 것.
- 마감이면 제목에 `⚠️ 마감`을 넣는다. 색과 제목 둘 다로 표시해야 색을 못 보는 화면에서도 구분된다.
- 설명에 **Discord 스레드 링크를 반드시 넣는다.** 폰에서 캘린더 알림을 받았을 때 한 번의 탭으로 작업을 이어갈 수 있어야 한다.
- 반복 일정은 사용자에게 주기를 확인한 뒤 만든다. 임의로 반복을 걸지 않는다.

---

## 작업 공간 규약 — 워크트리를 만들지 않는다

**이 프로젝트에서는 git 워크트리를 만들지 않는다.** `/Users/mh97888/business/projects/project-youtube-insight` 에서 직접 작업한다.
격리가 필요하다고 판단되더라도 만들지 않는다. 이 규약이 워크트리 관련 스킬·기본 동작보다 우선한다.

이유:

- **격리는 이미 되어 있다.** Discord 채널 하나 = 프로젝트 하나 = 디렉토리 하나이고, 봇이 채널별로 요청을 직렬화한다. 같은 채널의 두 요청이 동시에 파일을 건드리지 않는다.
- **워크트리는 작업을 숨긴다.** 결과가 별도 브랜치에 남으면 사용자는 메인에서 아무 변화도 못 본다. 폰으로 보는 사용자에게 "어느 워크트리의 어느 브랜치"는 전달되지 않는다.
- **정리되지 않고 쌓인다.** 실행마다 하나씩 생겨 한 프로젝트에 워크트리가 여러 개 붙고, 어느 것이 진짜 상태인지 알 수 없게 된다.

브랜치가 정말 필요하면 워크트리 대신 **같은 디렉토리에서 브랜치를 만들고**, 작업이 끝나면 메인으로 병합한 뒤 응답에 브랜치명을 명시한다.

기존에 만들어진 워크트리를 발견하면 지우지 말고 사용자에게 존재를 알린다.

---

## 하지 않는 것

- `git push --force`, 히스토리 재작성
- 사용자 확인 없이 외부에 메시지 전송, 결제, 주문
- `.env`, 토큰, 키를 Discord나 Notion에 출력 — 값이 필요하면 존재 여부만 보고한다
- 15분 넘게 걸릴 작업을 한 번에 시도 — 봇 타임아웃에 걸린다. 단계로 쪼개고 중간 상태를 `.agent-log.md`에 남긴다
